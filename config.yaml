# Updated Whitelist for Security Scanning

# This file includes known false positives that are allowed to be ignored during scans.
# Entries have been reviewed to ensure they do not include sensitive information.

whitelist:
  - hardcoded_api_key # Remove if sensitive data is detected
  - known_secret # Remove if sensitive data is detected
  - other_safe_entry_1
  - other_safe_entry_2
  # Add more entries as necessary, ensuring they are vetted for safety.
